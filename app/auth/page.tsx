"use client";
import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import { getSupabaseBrowserClient } from "@/lib/supabase-browser";
export default function AuthPage(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[ready,setReady]=useState(false);useEffect(()=>setReady(Boolean(getSupabaseBrowserClient())),[]);const submit=async(e:FormEvent)=>{e.preventDefault();const supabase=getSupabaseBrowserClient();if(!supabase){setMessage("Додайте ключі Supabase у .env.local і перезапустіть dev-сервер.");return}const{error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:window.location.origin+"/auth/callback"}});setMessage(error?error.message:"Посилання для входу надіслано на вашу пошту.")};return <div className="study-layout"><div className="eyebrow">Акаунт Wordly</div><h1 style={{fontSize:48}}>Синхронізуй свій прогрес.</h1><p>Увійдіть за magic link, щоб прогрес зберігався між пристроями.</p><form onSubmit={submit} className="panel" style={{textAlign:"left",marginTop:24}}><label htmlFor="email">Email</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" style={{display:"block",width:"100%",padding:14,margin:"8px 0 16px",border:"1px solid var(--line)",borderRadius:12}}/><button className="button" type="submit">Надіслати посилання</button>{message&&<p>{message}</p>}{!ready&&<p>Режим demo: спочатку додайте Supabase-ключі.</p>}</form><Link href="/" className="eyebrow" style={{display:"inline-block",marginTop:20}}>← До прогресу</Link></div>}
